spring.application.name=prescription-app

# H2 Database Configuration
spring.datasource.url=jdbc:h2:file:./data/prescriptiondb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# JWT Secret (for simplicity we might use Basic Auth or simple Session, but JWT is better. 
# Requirement says "Store user credentials in H2 DB", "Basic login form". 
# I will implement standard Spring Security with Session/Basic or JWT. 
# Given "Basic login form + secure routing", session-based with a custom login endpoint is fine, 
# but for a separated frontend, JWT or stateless Basic Auth is easier. 
# I'll stick to Basic Auth or a simple token approach if needed, but standard Session is tricky with CORS if not careful.
# Let's use Basic Auth for simplicity as per "Basic login form" requirement interpretation, 
# or actually, a simple Login Endpoint that returns user details is best for Angular.
